import { FC } from "react";
import { MovingBackgroundProps } from "./type";

const MovingBackground: FC<MovingBackgroundProps> = () => {
  return <h1>Moving Background</h1>;
};

export default MovingBackground;
